#!/usr/bin/env bash
# Legacy no-op: inspect lives in pymergetic-wasmmod-tools now.
set -euo pipefail
echo "OK: use pymergetic-wasmmod-tools (no CDN bundle to sync)."
echo "    pip install --pre 'pymergetic-wasmmod-tools>=0.1.0a1'"
echo "    # or editable: pip install -e ../wasmmod-tools"
