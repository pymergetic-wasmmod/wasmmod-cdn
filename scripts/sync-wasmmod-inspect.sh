#!/usr/bin/env bash
# Legacy: inspect helpers now come from pymergetic-wasmmod-tools (PyPI / editable).
# Kept so old docs/dev-up references do not fail.
set -euo pipefail
echo "OK: pymergetic-wasmmod-tools is the SoT (no CDN bundle to sync)."
echo "    pip install -e ../wasmmod-tools   # or: pip install pymergetic-wasmmod-tools"
