# Do NOT put stubs here for packages installed in .venv (or on extraPaths).
# Partial stubPath trees seal PEP 420 namespaces and cause
# "Import could not be resolved" even when the wheel is installed.
# Prefer: pip install -e ../wasmmod-tools  (+ pyrightconfig extraPaths).
